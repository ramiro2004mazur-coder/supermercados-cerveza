# El scraper de Carrefour/Dia pega contra el API publico de VTEX (catalog_system/pub)
# sin login ni API key, asi que hoy no hace falta ninguna variable de entorno.
# Este archivo queda como placeholder para cuando se sume una cadena que si
# necesite config sensible (ej. proxy si algun VTEX empieza a bloquear IPs de
# datacenter, o credenciales si alguna cadena futura exige login).
#
# PROXY_URL=
